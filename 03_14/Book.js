class Book {
    constructor(
        title,
        author,
        pageNum,
    ){
    this.title = title;
    this.author = author;
    this.pageNum = pageNum;
}
newBook(title, author, pageNum){
    this.title = title;
    this.author = author;
    this.pageNum = pageNum;
}};

export default Book;